name: Dart CI

on:
  push:
    branches: [ page-view-controller ]
  pull_request:
    branches: [ page-view-controller ]

jobs:
  build:

    runs-on: ubuntu-latest

    container:
      image:  google/dart:latest

    steps:
    - uses: actions/checkout@v2
    - name: Install dependencies
      run: flutter pub get
    - name: Run tests
      run: pub run test
